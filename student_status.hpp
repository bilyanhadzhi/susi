#ifndef STUDENT_STATUS_HPP
#define STUDENT_STATUS_HPP

//! The enum class StudentStatus holds possible statuses for each student, including the empty 'none'
enum class StudentStatus {none, active, interrupted, graduated};

#endif // STUDENT_STATUS_HPP
